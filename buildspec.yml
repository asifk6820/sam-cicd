version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - pip install --upgrade aws-sam-cli

  build:
    commands:
      - npm install
      - sam build --template sam.yml

artifacts:
  files:
    - packaged.yaml

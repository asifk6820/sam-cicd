version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14.x
    commands:
      - npm install
  build:
    commands:
      - npm run build
  deploy:
    commands:
      - sam build
  artifacts:
    files:
      - build/package.zip
